# OpenHear – Python dependencies
# Python 3.10+ required (Windows 11 primary target)
#
# Install everything with:
#   pip install -r requirements.txt
#
# Notes:
#   - PyAudio on Windows requires a pre-built wheel; install from
#     https://www.lfd.uci.edu/~gohlke/pythonlibs/#pyaudio or use
#     pipwin: `pip install pipwin && pipwin install pyaudio`
#   - hid wraps the hidapi C library; the binary wheel on PyPI includes
#     hidapi for Windows — no separate install needed.

# Audio I/O
PyAudio>=0.2.14
sounddevice>=0.5.2

# Numerical / DSP
numpy>=1.26.0
scipy>=1.12.0

# Plotting (audiogram visualiser)
matplotlib>=3.8.0

# USB HID (Noahlink Wireless 2)
hid>=1.0.6

# Lossless audio file loading (FLAC support for voice/reference.py)
# Optional: only needed if you load FLAC reference files.
soundfile>=0.12.1

# Wristband prototype transport
bleak>=1.1.1

# Wristband prototype inference backend
# Install either:
#   pip install tflite-runtime
# or:
#   pip install tensorflow
# then download a local YAMNet .tflite model; the official label CSV is
# already included at stream/data/yamnet_class_map.csv.
